@{ ViewBag.Title = "گزارش OCE"; }


@section scripts
{
    <script src="~/DX.ashx?jsfile=~/Views/OCEReport/Js/Index.js"></script>
}

<div class="row">
    <div class="col-xs-12 col-md-12">
        <section class="app__section">
            <div class="app__section__header">
                @ViewBag.Title
                @{ Html.RenderPartial("~/Views/Shared/Partials/_AppSectionSettings.cshtml"); }
            </div>
            <div class="app__section__body">
                @{ Html.RenderPartial("~/Views/OCEReport/FilterForm/FilterForm.cshtml"); }
                <form id="resultGridParent" class="app__grid-wrapper">
                    @{ Html.RenderPartial("~/Views/OCEReport/Grid/ResultGrid.cshtml"); }
                </form>
            </div>
        </section>
    </div>
</div>