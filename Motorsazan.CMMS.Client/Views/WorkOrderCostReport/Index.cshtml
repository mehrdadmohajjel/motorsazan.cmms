@{
    ViewBag.Title = "دریافت لیست گزارش هزینه هر سفارشکار براساس شرایط";
}

@section scripts
{
    <script src="~/DX.ashx?jsfile=~/Views/WorkOrderCostReport/Js/Index.js"></script>
}

<div class="row">
    <div class="col-xs-12 col-md-12">
        <section class="app__section">
            <div class="app__section__header">
                دریافت لیست گزارش هزینه هر سفارشکار براساس شرایط
                @{ Html.RenderPartial("~/Views/Shared/Partials/_AppSectionSettings.cshtml"); }
            </div>
            <div class="app__section__body">
                @{ Html.RenderPartial("~/Views/WorkOrderCostReport/FilterForm/FilterForm.cshtml"); }
                <form id="gridParent" class="app__grid-wrapper">
                    @{ Html.RenderPartial("~/Views/WorkOrderCostReport/Grid/Grid.cshtml"); }
                </form>
            </div>
        </section>
    </div>
</div>