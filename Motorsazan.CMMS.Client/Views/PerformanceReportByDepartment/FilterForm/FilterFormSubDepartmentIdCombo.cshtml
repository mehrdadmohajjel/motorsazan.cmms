@model Motorsazan.CMMS.Shared.Models.Output.Inspection.OutputGetSubDepartmentListByMainDepartmentId[]

@Html.DevExpress().ComboBox(
    settings =>
    {
        settings.Name = "filterFormSubDepartmentIdCombo";
        settings.CallbackRouteValues = new { Controller = "PerformanceReportByDepartment", Action = "FilterFormSubDepartmentListByMainDepartmentId" };
        
        settings.Properties.ClientSideEvents.BeginCallback =
            "motorsazanClient.performanceReportByDepartment.handleFilterFormSubDepartmentIdComboBeginCallback";

        settings.Width = Unit.Percentage(100);
        settings.Properties.ValidationSettings.ErrorDisplayMode = ErrorDisplayMode.None;
        settings.Properties.NullText = "نام خط را وارد کنید.";
        settings.SelectedIndex = -1;
        settings.Properties.CallbackPageSize = 100;
        settings.Properties.IncrementalFilteringMode = IncrementalFilteringMode.Contains;
        settings.Properties.IncrementalFilteringDelay = 200;
        settings.SettingsLoadingPanel.Text = "در حال دریافت اطلاعات";

        settings.Properties.ValueField = "DepartmentId";
        settings.Properties.ValueType = typeof(int);

        settings.Properties.TextField = "Title";

        settings.Properties.ClientSideEvents.SelectedIndexChanged =
            "motorsazanClient.performanceReportByDepartment.handleFilterFormSubDepartmentIdComboSelectedIndexChange";

    }).BindList(Model).GetHtml()