
@model Motorsazan.CMMS.Shared.Models.Output.Machine.OutputGetMachineTypeList[]

@Html.DevExpress().ComboBox(
settings =>
{
    settings.Name = "addFormMachineTypeCombo";
    settings.CallbackRouteValues = new { Controller = "Machine", Action = "AddFormMachineTypeCombo" };
    settings.Width = Unit.Percentage(100);
    settings.Properties.ValidationSettings.ErrorDisplayMode = ErrorDisplayMode.None;
    settings.Properties.NullText = "نوع دستگاه را انتخاب کنید";
    settings.SelectedIndex = -1;
    settings.Properties.CallbackPageSize = 100;
    settings.Properties.IncrementalFilteringMode = IncrementalFilteringMode.Contains;
    settings.Properties.IncrementalFilteringDelay = 200;
    settings.SettingsLoadingPanel.Text = "در حال دریافت اطلاعات";

    settings.Properties.ValueField = "MachineTypeId";
    settings.Properties.TextField = "MachineTypeName";
    settings.Properties.ValueType = typeof(long);

    settings.Properties.ClientSideEvents.SelectedIndexChanged =
        "motorsazanClient.machine.handleAddFormMachineTypeComboSelectedIndexChanged";

}).BindList(Model).GetHtml()


